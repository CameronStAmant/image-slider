const nextButton = document.getElementById('nextImage');
nextButton.addEventListener('click', () => {
  const next = document.getElementById('frame');
  // if (next.style.right === '0px') {
  next.classList.add('slidein');
  // next.style.right = '200px';
  // }
});

nextButton;
